extends layout 

block content 

    h1= title 

    p #[strong Do you really want to delete the item?]
    form(method='POST', action='')
        div.form-group
            input#itemid.form-control(type='hidden', name='itemid', required='true', value=item._id)
            button.btn.btn-danger(type='submit') Delete